import React from 'react'

const Header = () => {
    return(
        <div>
            connecting with App.jsx or 6th file
            work as navbar but can be used more than once
        </div>
    )
}

export default Header